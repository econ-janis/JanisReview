{
  "name": "oms-auditor-vercel",
  "version": "0.2.0",
  "private": true,
  "description": "OMS Capability Auditor — variante Vercel, on-demand: el usuario tipea appKey/appSecret/janisClient en el dashboard, nada se persiste.",
  "scripts": {
    "dev": "vercel dev",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {},
  "engines": {
    "node": ">=18"
  }
}
