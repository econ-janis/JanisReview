{
  "name": "oms-auditor-vercel",
  "version": "0.1.0",
  "private": true,
  "description": "OMS Capability Auditor — variante Vercel (Serverless Functions + Postgres), en vez de AWS SAM.",
  "scripts": {
    "dev": "vercel dev",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.600.0",
    "@neondatabase/serverless": "^1.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}
